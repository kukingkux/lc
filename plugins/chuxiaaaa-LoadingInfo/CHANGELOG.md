## 1.1.4

Fix the issue of incorrect player display

## 1.1.3

v80!!!

## 1.1.2

Fix some bugs

## 1.1.1

Fixes the issue where client-side player display is inconsistent with the host.

## 1.1.0

Try to Fix the issue where loading text may still be displayed after loading is complete.

## 1.0.9

Fixed the bug where message data might not be hidden

## 1.0.8

- Fixed the issue where the displayed waiting player name was incorrect when returning to the orbit.
- Fixed the problem that after an employee was dismissed, the client would keep a black overlay until the spaceship landed.
- LoadingInfo will no longer affect the chat interface (if the client does not have LoadingInfo installed, the chat messages may be covered by hidden data text).

## 1.0.7

Fix the issue of incorrect player display

## 1.0.6

When there is only one player in the lobby, the message notification about the loading progress will no longer be sent.

## 1.0.5

Attempt to fix NullReferenceException

## 1.0.4

A new configuration item, NameMaxLength, has been added. You can modify it to limit the maximum display length of the name, with a default value of -1. 

Client support has been added; when the host has this mod installed, it will also take effect on the client.

## 1.0.3

Added a new configuration category `LoadTipCompat`.

You can customize the positions of the two hint texts by modifying `LoadTextY` and `LoadingTipsY`.

The above configuration only takes effect when the `Enova.LoadingTips` mod installed.

## 1.0.2

Fix the issue where the text of Enova.LoadingTips is moved off the screen under certain resolutions.

## 1.0.1

Compatible with the Enova.LoadingTips. 

Display up to 3 players who are still loading, and the maximum display length of their names is 5 characters.

## 1.0.0

Released

发布
