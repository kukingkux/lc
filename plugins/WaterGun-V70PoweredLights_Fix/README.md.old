# About
This mod is made to fix the new tiles that were introduced in the v70 update, the problem is simple zeekerss incorrectly added tags to the lights which made them not disable if breaker is disabled

# Technical
This mod will be depracated if zeekerss fixes the bug